= render "layouts/sidebar"
#right
  = render "layouts/hamburger"
  = render "layouts/top", :top => "My Classes"
  - @events.each do |x|
    = link_to event_path(x) do
      .imageSection.clip
        = image_tag x.cover.url
        .gradient
          %span
            = x.title