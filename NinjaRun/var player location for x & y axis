var player location for x & y axis

var x star location on x axis

when y = x hit

function input(inputType, DOMElement, callback){
	DOMElemet.addEventListener(inputType, function(event){
		var x = event.clientX;
		var y = event.clientY;
		callback(DOMElement, x,y);
	});
}

